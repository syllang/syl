#!/usr/bin/env bash

set -euo pipefail

if [ "$#" -lt 1 ]; then
  exit 0
fi

repo_root="$(git rev-parse --show-toplevel)"
exec python3 "$repo_root/scripts/validate_commit_scope.py" --repo "$repo_root" commit-msg "$1"
